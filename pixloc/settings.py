from pathlib import Path

# root = Path(__file__).parent.parent  # top-level directory
# DATA_PATH = root / 'datasets/'  # datasets and pretrained weights
# TRAINING_PATH = root / 'outputs/training/'  # training checkpoints
# LOC_PATH = root / 'outputs/hloc/'  # localization logs
# EVAL_PATH = root / 'outputs/results/'  # evaluation results

root = '/ws/external'  # top-level directory
DATA_PATH = '/ws/data/kitti-vo'  # datasets and pretrained weights
TRAINING_PATH = '/ws/external/outputs/training/'  # training checkpoints
LOC_PATH = '/ws/external/outputs/hloc/'  # localization logs
EVAL_PATH = '/ws/external/outputs/results/'  # evaluation results
